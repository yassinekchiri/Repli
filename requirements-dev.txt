# netapp-migration — test tooling (not needed to run the tool itself)
#
# Same rule as requirements.txt: floor + ceiling, so that an install on
# Python 3.9 cannot be dragged onto a release that dropped it.
#
#     pip install --no-index --find-links wheels/ -r requirements-dev.txt
#
# The suite is fully offline: no cluster is ever contacted, everything runs
# against the in-memory FakeClient in tests/conftest.py.
#
#     pytest

-r requirements.txt

# pytest 9.x requires Python >= 3.10.
pytest>=7.4,<9                  # bundled: 8.4.2 (pluggy 1.6.0, iniconfig 2.1.0)

# Required by fastapi.testclient to drive the API without a live server.
httpx>=0.24,<0.29               # bundled: 0.28.1 (httpcore 1.0.9, h11 0.16.0)
